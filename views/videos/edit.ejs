<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Vídeo</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color:  #ede7da;
        padding-top: 50px;
      }
      .container {
        max-width: 800px;
        margin: auto;
        background: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      }
      .button-group {
        display: flex;
        justify-content: center; /* Centra os botões */
        gap: 1rem; /* Espaçamento entre os botões */
      }
      .btn-cancelar {
        margin-top: 1rem;
        background-color: #6c757d; /* Cor cinza para botão cancelar */
        color: white;
      }
      .btn-atualizar {
        margin-top: 1rem;
        background-color: #981718; /* Cor verde para botão atualizar */
        color: white;
      }

      .icon {
        color: #981718;
        margin-right: 0.5rem;
      }
      h1 {
        color: #981718;
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center">
        <i class="fa-solid fa-video icon"></i> Editar Vídeo
      </h1>
      <form action="/videos/<%= video._id %>?_method=PUT" method="POST">
        <div class="mb-3">
          <label for="title" class="form-label">Título do Vídeo</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fas fa-info icon"></i></span>
            <input type="text" class="form-control" id="title" name="title" value="<%= video.title %>" required>
          </div>
        </div>
        <div class="mb-3">
          <label for="description" class="form-label">Descrição</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fa-solid fa-comment icon"></i></span>
            <textarea class="form-control" id="description" name="description" rows="3" required><%= video.description.trim() %></textarea>
          </div>
        </div>
        
        <div class="mb-3">
          <label for="video_url" class="form-label">URL do Vídeo</label>
          <div class="input-group">
            <span class="input-group-text"><i class="fa-solid fa-link icon"></i></span>
            <input type="url" class="form-control" id="video_url" name="video_url" value="<%= video.video_url %>" required>
          </div>
        </div>
        
        <div class="mb-3">
          <label for="thumbnail_url" class="form-label">URL do Thumbnail</label>
          <div class="input-group">
          <span class="input-group-text"><i class="fa-solid fa-link icon"></i></span>
          <input
            type="url"
            class="form-control"
            id="thumbnail_url"
            name="thumbnail_url"
            value="<%= video.thumbnail_url %>"
            required
          />
        </div>
        </div>
        <div class="button-group">
          <button type="submit" class="btn btn-atualizar">
            <i class="fa-solid fa-check icon"></i> Atualizar Vídeo
          </button>
          <button
            type="button"
            class="btn btn-cancelar"
            onclick="window.history.back();"
          >
            <i class="fa-solid fa-arrow-left icon"></i> Cancelar
          </button>
        </div>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
