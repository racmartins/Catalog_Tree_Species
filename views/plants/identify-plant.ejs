<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Identificar Planta</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />
    <style>
        body {
            background-color: #ede7da; /* Fundo mais claro para melhor contraste */
            padding-top: 20px;
        }
        .container {
            max-width: 500px;
            margin: 20px auto;
            padding: 20px;
            background: #fff;
            border-radius: 15px; /* Bordas mais arredondadas */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Sombra sutil */
        }
        h2 {
            color: #981718; /* Cor do título */
        }
        .btn-outline-success {
        color: #981718 !important; /* Muda a cor do texto para vermelho */
        border-color: #981718 !important; /* Muda a cor da borda para vermelho */
      }

      .btn-outline-success:hover {
        color: #fff; /* Muda a cor do texto para branco ao passar o mouse */
        background-color: #981718 !important; /* Muda o fundo para vermelho ao passar o mouse */
        border-color: #981718 !important; /* Muda a cor da borda para vermelho ao passar o mouse */
        color:white !important;
      }
        .form-label em {
            color: #666; /* Cor mais suave para o label */
        }
        .icon-field i {
            margin-right: 5px;
        }
    </style>
</head>
<body>
<div class="container shadow">
    <h2 class="text-center mb-4">Identifique a sua Planta</h2>
    <form action="/plants/identify-plant" method="post" enctype="multipart/form-data" class="needs-validation" novalidate>
        <div class="mb-3">
            <label for="plantImage" class="form-label"><i class="fas fa-upload icon-field"></i><em>Selecione uma imagem</em></label>
            <input class="form-control" type="file" id="plantImage" name="plantImage" accept="image/*" required />
            <div class="invalid-feedback">Por favor, selecione uma imagem válida.</div>
        </div>
        <div class="d-grid gap-2">
            <button type="submit" class="btn btn-outline-success">
                <i class="fas fa-search"></i> Identificar Planta
            </button>
            <button type="button" class="btn btn-outline-secondary" onclick="window.history.back();">
                <i class="fas fa-times"></i> Cancelar
            </button>
        </div>
    </form>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Exemplo de JavaScript para desativar o envio de formulários se houver campos inválidos
    (function () {
      "use strict";
      var forms = document.querySelectorAll(".needs-validation");
      Array.prototype.slice.call(forms).forEach(function (form) {
        form.addEventListener(
          "submit",
          function (event) {
            if (!form.checkValidity()) {
              event.preventDefault();
              event.stopPropagation();
            }
            form.classList.add("was-validated");
          },
          false
        );
      });
    })();
  </script>
</body>
</html>
