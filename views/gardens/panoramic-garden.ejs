<!DOCTYPE html>
<html>
  <head>
    <title>Vista Panorâmica do Jardim</title>
    <script src="https://aframe.io/releases/1.2.0/aframe.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <style>
      /* Estilos para melhorar a visualização */
      body {
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #f7f7f7;
      }
      h2 {
        position: absolute;
        top: 20px;
        left: 50%;
        transform: translateX(-50%);
        color: #981718;
      }
      .btn-back {
        position: absolute;
        bottom: 20px;
        left: 50%; /* Centra horizontalmente */
        transform: translateX(-50%); /* Desloca o botão para esquerda pela metade de sua largura */
      }
    </style>
  </head>
  <body>
    <h2>Explore o nosso Jardim em 360°</h2>

    <!-- Incorporação da vista panorâmica -->
    <a-scene embedded style="width: 90%; height: 80vh">
      <a-sky src="<%= panoramicImageUrl %>" rotation="0 -130 0"></a-sky>
    </a-scene>

    <!-- Botão de voltar centralizado na parte inferior -->
    <button onclick="history.back()" class="btn btn-outline-secondary btn-back">Cancelar</button>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

